// Package shared defines the public bundle SDK contract shared by bundle
// constructors and implementations.
//
// It owns provisioner configuration, process overrides, provisioner-applied
// mounts, descriptors, and the Provisioner interface.
package shared
