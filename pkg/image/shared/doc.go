// Package shared defines the public image SDK contract shared by image
// constructors and implementations.
//
// It owns pull requests, platform/auth/policy fields, pulled-image metadata,
// layout validation, and deterministic layout paths for canonical image
// references.
package shared
